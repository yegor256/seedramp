%div.logo
  %a{ :href=>'/' }
    - if amp
      %amp-img{ :src=>'/images/logo.svg?REVISION', :alt=>'logo', :width=>"100", :height=>"100" }
    - else
      %img{ :src=>'/images/logo.svg?REVISION', :alt=>'logo', :class=>'noprint' }
